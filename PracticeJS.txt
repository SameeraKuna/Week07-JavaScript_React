//Task-1
let name = prompt("Enter name");
let age = prompt("Enter age");
let city = ("Enter city");
document.getElementById("display").innerHTML= 
`   <p>Name: ${name}</p>
    <p>Age: ${age}</p>
    <p>City: ${city}</p> `


//Task2

function analyze(){
let input = document.getElementById("string")
let str = input.value.trim();
let error = document.getElementById("error");

if(str === "") {error.innerHTML = `<small>Enter a valid string</small>`;
   error.style.display = "block";
   error.style.color = "red";
   input.style.borderColor = "red";
   output.innerHTML = ""
   output.style.border = "none";
return;
}
else{
    error.textContent = "";
    error.style.display = "none";
    input.style.borderColor = "gray";
}
let len = str.length;
let upper = str.toUpperCase();
let lower = str.toLowerCase();
let start3 = str.slice(0,3);
let end3 = str.slice(len-3, len); //slice(-3)
document.getElementById("output").innerHTML=
`<div class = "outputstyle">Length of given string: ${len} <br />
String in uppercase: ${upper} <br />
String in lowercase: ${lower} <br />
First three letters in string: ${start3} <br />
Last three letters in string: ${end3}
</div>`}


